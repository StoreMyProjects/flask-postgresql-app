# create slack webhooks secret for alertmanager

kubectl create secret generic alertmanager-slack-critical \
  -n monitoring \
  --from-literal=webhook=<webhook-url>


kubectl create secret generic alertmanager-slack-warning \
  -n monitoring \
  --from-literal=webhook=<webhook-url>

